import { title } from "@/components/primitives";

// Substitua este conteúdo pelos seus cards e gráficos
export default function AnalisePerformance() {
    return (
        <div className="w-full mt-6">
            <h2 className={title({ size: "sm" })}>Visão Geral de Performance</h2>
            {/* TODO: Adicionar aqui os cards e gráficos da Visão Geral */}
        </div>
    )
}